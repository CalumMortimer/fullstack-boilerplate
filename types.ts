// this is an example of a types file, it should be located at the root of all applications
// in order to keep types identical between the client and the server
export type User = {
    id: string;
    name: string;
};